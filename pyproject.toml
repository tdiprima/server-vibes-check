[project]
name = "server-vibes-check"
version = "0.1.0"
description = "Server vibes check"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "loguru>=0.7.3",
    "psutil>=7.2.1",
    "schedule>=1.2.2",
]
